import styled from 'styled-components';

export const Container = styled.div`
  width: 100vw;
  height: 100vh;
  margin: 0 auto;

  padding: 8px;

  display: grid;

  grid-template-columns: 1fr;
  grid-gap: 24px;
`;
